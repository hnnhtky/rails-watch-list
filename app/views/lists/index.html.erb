<h1> Movie List </h1>

<ul>
  <% @lists.each do |list| %>
    <li><%= linkt_to list.name, list_path(list) %></li>
  <% end %>
</ul>

<%= link_to "Create a new list", new_list_path, class: "btn btn-primary" %>
